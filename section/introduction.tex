\section{Introduction}
\label{sec:intro}
    The rapid technological advancement in railway systems is driving a fundamental shift from manual human control to autonomous operations. This transformation is particularly evident in the pursuit of higher grades of automation, i.e. GoA levels 3 and 4, where trains operate with minimal human intervention or become completely driverless. As railway systems progress toward full automation, the role of remote control systems becomes increasingly critical. The Remote control system is a vital layer in railway operations, providing essential fallback capabilities when autonomous systems encounter failures \cite{Shift2Rail2019, CoganMilius23}.

    To fulfill this critical fallback role effectively, a remote control system must support two fundamental communication channels: one for transmitting control commands and another for streaming real-time video. In most existing teleoperated systems, control commands are typically transmitted using protocols such as MQTT\footnote{MQTT: Message Queuing Telemetry Transport} or WebSocket\footnote{WebSocket: Bidirectional communication channel over TCP}, while video streaming is handled by UDP-based protocols like RTSP\footnote{RTSP: Real Time Streaming Protocol} or WebRTC\footnote{WebRTC: Web Real-Time Communication} \cite{MejiasDZRAI24, SunTY25, FezeuCZDVBSNJRGZ26}.
    TCP's strict in-order deliver causes Head-of-Line (HoL) blocking. A single lost packet stalls subsequent control commands until retransmission occurs, introducing unpredictable latency spikes \cite{Langley2017QUIC}. WebRTC, while designed for low-latency media streaming, add extra layer of architectural complexity by requiring a separate signaling server and startup latency for connection establishment \cite{Mejias2025QUICWebRTC}.

    WebTransport is built on the QUIC protocol, standardized as the foundation for HTTP/3, a promising alternative to both WebSocket and WebRTC protocols for time-centric reactive software systems. It offers supports for both reliable streams for lossless control commands and unreliable datagrams for latency-sensitive video streaming, making it well-suited for remote control applications in railway operations.\cite{AmetLY25, rfc9000}.

    Remote Control System includes physical components such as sensors and actuators. Data exchange and coordination among these components are commonly implemented using mechanisms such as the publish–subscribe pattern or callback-based execution. While widely adopted, these approaches can introduce non-deterministic behavior due to unpredictable callback ordering and concurrent event handling. To achieve deterministic execution in this cyber-physical environment, the train client is implemented using Lingua Franca, a reactor-oriented coordination language that guarantees determinism by construction. Lingua Franca decouples logical time from physical wall-clock time, enabling predictable and reproducible execution semantics \cite{LohstrohMBL21}. This design eliminates race conditions and the non-deterministic callback ordering often associated with traditional publish–subscribe mechanism, thereby ensuring consistent and predictable execution traces on the physical train system.


    This paper provides following contribution:
        \begin{itemize}
            \item The architectural design of a train client using Lingua Franca

            \item A performance analysis of QUIC based WebTransport in the context of remote control system under realistic network conditions.
        \end{itemize}